{
  "name": "pokedex-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.ts",
    "build": "npm run build:client && vite build",
    "start:dist": "node dist/server.js",
    "dev": "concurrently -n vite,node -c cyan,green \"vite build --watch\" \"node --watch dist/server.js\"",
    "dev:fast": "node --watch server.ts",
    "test": "npm run build:client && POKEDEX_NO_RETRY=1 node --test",
    "typecheck": "tsc --noEmit",
    "build:client": "vite build --config vite.client.config.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@christopherscot/pokedex-client": "^0.6.0",
    "fastify": "^5.12.5",
    "prom-client": "^15.1.3"
  }
}
